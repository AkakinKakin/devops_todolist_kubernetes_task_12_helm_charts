apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: {{ .Values.common.namespace }}
type: Opaque
data:
{{- range $key, $value := .Values.todoapp.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
{{- end }}
